# Required environment variables for Workout Log API

# JWT secret key for token signing (min 32 characters)
JWT_SECRET=your-super-secret-key-change-in-production

# Database connection URL
DATABASE_URL=sqlite:///./workout.db

# Optional: Bcrypt rounds for password hashing (default: 12)
# BCRYPT_ROUNDS=12
